<% if logged_in? %>
  <div class="container-fluid">
    <div class="row">
      <div class="col-xs-12">
        <%= render 'feed' %>
      </div>
    </div>
  </div>

<% else %>
  <%= render 'menus/signup' %>
<% end %>