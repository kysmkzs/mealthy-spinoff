<%= link_to "" , 
    favorite_path(current_user.favos.find_by(:menu_id => menu.id) , 
      menu_id: menu.id , type: "Favo") ,
    :class => "favo-#{menu.id} btn-favo btn btn-danger btn-sm favoed",
    :remote => true,
    :method => :delete %>