  <div class="row">
    <div class="col-xs-12">
      <div class="container">
      <div class="container-inner inner-default clearfix">
        <% @menus.each do |menu| %>
          <div class="item-box">
            <div class="image">
              <div class="img-wrap">
              <p><%= render 'users/like_form' , menu: menu if logged_in? %></p>
              <p><%= image_tag menu.image.url %></p>
              </div>
            </div>
            <div class="detail">
              <p class="title">
                <%= menu.name %> <%= menu.calorie %>[kcal]
              </p>
            </div>
          </div>
        <% end %>
      </div>
      </div>
    </div>
  </div>